using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ButtonActive : MonoBehaviour
{
    public Button AttachedButton;

    
    public void CheckActive()
    {
        //if (AttachedButton.isActiveAndEnabled)
        //    AttachedButton.GetComponent<Image>().sprite = ButtonList.instance.Buttons[1];
        //else
        //    AttachedButton.GetComponent<Image>().sprite = ButtonList.instance.Buttons[0];
    }
}
